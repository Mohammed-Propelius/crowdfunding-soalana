import './App.css'

function App() {
  const checkIfWalletIsConnected = async () => {
    try { 
      const {solana} = window.
    }
    catch (e) {
      console.log(e)
    }
  }
}

export default App
